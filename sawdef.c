#include<stdio.h>
#include<math.h>
void main()
{
int min,array[i],i,n;
printf("enter the no.of elements");
scanf("%d",&n);
for(i=0;i<n;i++)
min=array[0];
scanf("%d",&array[i]);
if(min>array[i])
{
min=array[i];
i++;
}
printf("min element is %d",min);
}
 getch();
 }

